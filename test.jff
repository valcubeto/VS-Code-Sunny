varbl
  num = 0123456789;
  num = 1234e+5678;
const hex = 0x09abcdef;
const bin = 0b00011101;
const str = "Hello world!";
const boo = true;
const obj = { num }; // { num: num }
const arr = [ num, hex, bin ];
const rex = /my +regular +expression/i;

fun yommy(a, b) {
  return a + b;
}

class Pet {
  own priv count = 0;
  oncreate({ name, age }) {
    this.add(name, age);
    count++;
  }
  toString() => "%% { name: $(this.name), age: $(this.age) }" << this#class.name;
}

class Dog: Pet {
  woof() {
    print("$(this.name or "A dog"): Woof!");
  }
}

class Cat: Pet {
  meow() {
    print("$(this.name or "A cat"): Meow!");
  }
}

2 =(num)> num in 0 ~ 10 =(inRange)> if inRange: print("num in range!");