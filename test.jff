const BIN at &0xff = 0b100101001;

print(BIN AND 0b100101001);

operator not(y) -> Boolean {
  const bool = Boolean(y);
  if bool == true: return false;
  else: return true;
};

class Boolean {
  oncall(val: Value) {
    if typeof val == "boolean": return val;
    if typeof val == "number" and val == 0: return false;
  }
  oncreate(val) -> { String: Boolean } => { # Boolean, "VALUE": Boolean(val) }
}

class Array extends (class Object extends Value {}) {
  oncreate(x) {
    foreach value in x: print it;
  }
}
fu