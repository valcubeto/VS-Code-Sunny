varbl
  num = 0123456789;
  num = 12e+345.67;
const hex = 0x09abcdef;
const bin = 0b00011101;
const str = "Hello world!";
const boo = true;
const obj = { num }; // { num: num }
const arr = [ num, hex, bin ];
const rex = /my +regular +expression/i;

fun yommy(a, b) {
  return a + b;
}

class Pet {
  own priv count = 0;
  oncreate({ name, age }) {
    this.add(name, age);
    count++;
  }
  toString() => "Pet { name: $(this.name), age: $(this.age) }";
}